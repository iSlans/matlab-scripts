syms x [2 1]
f = x1 + x2;
g = [x1 ^ 2 + x2 ^ 2 - 2];
h = [x1 ^ 2 - x2];

sol = LKKTsystem(x, f, g, h);

% ------------------------------------ --- ----------------------------------- %

syms x [2 1]
f = x1 ^ 2 + 2 * x1 + x2 ^ 2 + 2 * x2;
g = [
    x1 + x2 ^ 2
    x1 ^ 2 - 4
    ];
h = [];
sol = LKKTsystem(x, f, g);

% ------------------------------------- - ------------------------------------ %

syms x [2 1]
f = -x1 ^ 2 + x2;
g = [];
h = [
    x1 ^ 2 + x2 ^ 2 + 2 * x2
    ];
sol = LKKTsystem(x, f, g, h);

% s.value(:,1) = subs(f, vars, s.x)
